# Keyring and auth
exec-once = gnome-keyring-daemon --start --components=secrets
exec-once = /usr/libexec/kf6/polkit-kde-authentication-agent-1  # was polkit-gnome (not packaged on Nobara); use installed KDE polkit agent

# Clipboard history
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# Auto delete trash 30 days old
exec-once = trash-empty 30

# Cursors
exec-once = hyprctl setcursor $cursorTheme $cursorSize
exec-once = gsettings set org.gnome.desktop.interface cursor-theme '$cursorTheme'
exec-once = gsettings set org.gnome.desktop.interface cursor-size $cursorSize

# Location provider and night light
exec-once = /usr/libexec/geoclue-2.0/demos/agent  # path is /usr/libexec on Fedora 43, not /usr/lib
# exec-once = sleep 1 && gammastep   # night light disabled — was auto-warming on sunset/geoclue schedule. Uncomment to re-enable.

# Idle inhibit when audio playing
exec-once = sway-audio-idle-inhibit --ignore-source-outputs cava

# Forward bluetooth media commands to MPRIS
exec-once = mpris-proxy

# Resize and move windows based on matches (e.g. pip)
exec-once = caelestia resizer -d

# Start shell
exec-once = caelestia shell -d

# Idle daemon
exec-once = hypridle
